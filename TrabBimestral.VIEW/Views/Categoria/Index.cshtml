@model IEnumerable<TrabBimestral.MODEL.Categoria>

@{
    ViewBag.Title = "Categorias";
}

<h2>Categorias</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cat_Nome)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cat_Nome)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Cat_ID }) |
                @Html.ActionLink("Detalhes", "Details", new { id = item.Cat_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Cat_ID }, new { @class = "excluir" })
            </td>
        </tr>
    }

</table>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>
        $(document).ready(function () {

        })
        $(".excluir").click(function () {

            var apagar = confirm("Deseja realmente excluir este registro ?");
            if (apagar) {
                // aqui vai a instrução para apagar registro

            } else {
                event.preventDefault();
            }
        });
    </script>
}